server:
  port: 13000
spring:
  profiles:
    active: dev
  application:
    name: heltnote-api

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost